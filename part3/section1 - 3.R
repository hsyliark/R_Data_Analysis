setwd("D:/R_ECSHIN/part3/data")
getwd()


print(pi,digits=5)  # 숫자표시 갯수 조절 (digits)

print(3,4)     #숫자가 하나만 찍힘 
print('a','b') # error 발생

cat(1,':','a','\n',2,':','b') #cat 명령어는 1개이상의 문자및 숫자열을 print 해준다 : catenate

cat(3,4)
3;4;5;

1+2; 3*4; pi*5*5;
